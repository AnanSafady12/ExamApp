# הערות פיתוח וסיכום אישי (Personal Development Notes)

קובץ זה מכיל סיכום והערות אישיות על הפיתוח של מערכת המבחנים (ExamApp).

## דגשים טכנולוגיים וארכיטקטורה
- **רכיבים מודולריים (Modular Design)**: הפרדת דפי הפורטל (כמו StudentPortal ו-TeacherDashboard) לתתי-רכיבים ייעודיים (כמו StudentExamList, QuestionCard, ו-ExamTakingView) תורמת לקריאות הקוד, להקלה בבדיקות אוטומטיות, ולמניעת כפילויות.
- **שירותים מנותקי דומיין (Generic Services)**: השירותים הגנריים כמו StorageService, LoggerService, ו-NotificationService נכתבו בגישת OOP נקייה וללא תלות בלוגיקה של המערכת, מה שמאפשר להעתיק אותם בקלות לפרויקטים אחרים.
- **ניהול מצבים (Exam Status Flow)**: המצבים `draft`, `published`, ו-`closed` מנוהלים בצורה הדוקה. ישנה בדיקה כפולה בזמן פתיחת המבחן ובזמן ההגשה כדי למנוע הגשות של מבחנים שנסגרו או שונו על ידי המורה בזמן אמת.
- **בדיקות אוטומטיות (Vitest)**: כל Flows הבדיקות (מערכת ההרשאות, יצירת מבחן, הגשת תשובות, שינוי סטטוס, והשירותים הגנריים) מכוסים במלואם תחת סביבת `jsdom` מהירה ויציבה.
